import ClickableIcon from "./ClickableIcon";

export default function AboutMeWindow() {
    return (
        <div className="flex w-full h-full p-3 bg-white rounded-b-md text-[#333031]">
            i will finish this if i have no job and i need to start applying again
        </div>
    );
}